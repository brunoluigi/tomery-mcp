<div id="<%= dom_id user %>" class="w-full sm:w-auto my-5 space-y-5">
  <p class="font-bold"> <%= user.email_address %> | <%= user.active? ? "active" : "<span class='text-alert opacity-75'>inactive</span>".html_safe %> | <%= user.admin? ? "admin" : "mortal" %> </p>
</div>
